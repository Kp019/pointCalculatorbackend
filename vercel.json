{
  "devCommand": "uvicorn main:app --host 0.0.0.0 --port 9000",
  "installCommand": "pip install -r requirements.txt",
  "framework": "fastapi",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
